import { GetValuesPipePipe } from './get-values-pipe.pipe';

describe('GetValuesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new GetValuesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
